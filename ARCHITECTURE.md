# 书签导航 - 前后台架构设计

## 📋 技术方案

### 技术栈
- **前端**: HTML5 + CSS3 + Vanilla JavaScript
- **后端**: Cloudflare Workers
- **数据存储**: Cloudflare KV
- **认证**: 简单密码保护(HTTP Basic Auth)

## 🏗️ 架构设计

### 1. 前台导航页面 (`index.html`)
**功能**:
- 展示分类书签卡片
- 实时搜索功能
- 快捷搜索入口(Google、Bing、百度)
- 响应式设计
- 从KV读取数据展示

**路由**: `/`

### 2. 后台管理页面 (`admin.html`)
**功能**:
- 书签增删改查(CRUD)
- 分类管理
- 批量导入HTML书签文件
- 数据导出功能
- 简单密码登录

**路由**: `/admin`

### 3. API接口 (Cloudflare Workers)

#### 数据API
- `GET /api/bookmarks` - 获取所有书签
- `POST /api/bookmarks` - 创建书签
- `PUT /api/bookmarks/:id` - 更新书签
- `DELETE /api/bookmarks/:id` - 删除书签
- `POST /api/bookmarks/import` - 批量导入

#### 认证API
- `POST /api/auth/login` - 登录验证
- `POST /api/auth/verify` - 验证token

## 💾 数据结构

### KV存储结构
```javascript
// Key: bookmarks
{
  "categories": [
    {
      "id": "cat_1",
      "name": "工具",
      "icon": "🔧",
      "order": 1
    }
  ],
  "bookmarks": [
    {
      "id": "bm_1",
      "title": "GitHub",
      "url": "https://github.com",
      "icon": "https://github.com/favicon.ico",
      "categoryId": "cat_1",
      "order": 1,
      "createdAt": "2024-01-01T00:00:00Z",
      "updatedAt": "2024-01-01T00:00:00Z"
    }
  ]
}

// Key: admin_password
{
  "hash": "hashed_password"
}

// Key: auth_tokens (用于会话管理)
{
  "token_abc123": {
    "createdAt": "2024-01-01T00:00:00Z",
    "expiresAt": "2024-01-02T00:00:00Z"
  }
}
```

## 🔐 安全设计

### 后台访问控制
1. 首次访问 `/admin` 需要输入密码
2. 验证通过后生成JWT token存储在localStorage
3. 后续API请求携带token进行验证
4. token有效期24小时

### API保护
- 所有 `/api/bookmarks/*` 写操作需要验证token
- 读操作 `/api/bookmarks` (GET) 公开访问
- 管理员API需要密码验证

## 📁 文件结构

```
bookmark-navigator/
├── public/
│   ├── index.html          # 前台导航页面
│   ├── admin.html          # 后台管理页面
│   ├── css/
│   │   ├── main.css        # 前台样式
│   │   └── admin.css       # 后台样式
│   └── js/
│       ├── main.js         # 前台逻辑
│       └── admin.js        # 后台逻辑
├── src/
│   └── worker.js           # Cloudflare Workers主文件
├── wrangler.toml           # Cloudflare配置
└── README.md               # 使用说明
```

## 🚀 部署流程

### 1. 配置Cloudflare KV
```bash
# 创建KV命名空间
wrangler kv:namespace create "BOOKMARKS"

# 绑定到wrangler.toml
[[kv_namespaces]]
binding = "BOOKMARKS"
id = "your-namespace-id"
```

### 2. 设置管理员密码
```bash
# 使用wrangler设置密码
wrangler secret put ADMIN_PASSWORD
```

### 3. 部署Worker
```bash
wrangler deploy
```

## 🎨 前台搜索站点设计

### 搜索入口布局
```
┌─────────────────────────────────────┐
│     🔍 快速搜索                      │
│  ┌─────────────────────────────┐   │
│  │ 输入关键词搜索...            │   │
│  └─────────────────────────────┘   │
│                                     │
│  [🌐 Google] [🔵 Bing] [🔴 百度]  │
└─────────────────────────────────────┘
```

### 搜索功能
- 输入框实时搜索书签
- 点击搜索引擎按钮跳转到对应搜索页
- 支持在搜索框回车使用默认搜索引擎

## 🔄 数据流

### 前台加载流程
1. 页面加载 → 调用 `GET /api/bookmarks`
2. 渲染分类和书签卡片
3. 初始化搜索功能

### 后台管理流程
1. 访问 `/admin` → 检查token
2. 未登录 → 显示登录表单
3. 登录成功 → 加载管理界面
4. 操作书签 → 调用API → 更新KV → 刷新界面

## 📝 下一步实现

1. ✅ 完成架构设计
2. ⏳ 创建前台导航页面
3. ⏳ 创建后台管理页面
4. ⏳ 实现Worker API
5. ⏳ 配置KV存储
6. ⏳ 部署测试